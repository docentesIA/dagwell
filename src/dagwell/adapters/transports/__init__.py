"""Transport implementations — shared infrastructure, never per-platform
(spec §2). v1 implements subprocess only; http/sdk/mcp/a2a are reserved
names awaiting their own gated spec extensions (§6.1)."""
